export interface Song {
  id: string;
  title: string;
  artist: string;
  image: string;
  audio?: number;
}

export interface FeaturedItem {
  id: string;
  title: string;
  subtitle: string;
  image: string;
}
